server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/threshold
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  kafka:
    consumer:
      group-id: images.wip-consumer-group-3
    producer:
      transaction-id-prefix: images.done-producer-3